package com.member.util;

import org.springframework.stereotype.Component;

//import io.jsonwebtoken.Claims;
//import io.jsonwebtoken.Jwts;

@Component
public class JwtUtil {
	/*
	 * private final String SECRET = "MySuperSecretKeyForJwtWhichIsVerySecure12345";
	 * 
	 * public Claims extractClaims(String token) { return Jwts.parserBuilder()
	 * .setSigningKey(SECRET.getBytes()) .build() .parseClaimsJws(token) .getBody();
	 * 
	 * 
	 * }
	 */
}
